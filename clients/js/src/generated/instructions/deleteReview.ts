/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findDomainPda, findReviewPda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  expectSome,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DeleteReviewInstructionAccounts = {
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The account reviewing the domain */
  reviewer: Signer;
  /** The domain PDA. Seeds: ['domain', domain string] */
  domain?: PublicKey | Pda;
  /** The review PDA. Seeds: ['review', domain PDA, reviewer] */
  review?: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type DeleteReviewInstructionData = { discriminator: number };

export type DeleteReviewInstructionDataArgs = {};

export function getDeleteReviewInstructionDataSerializer(): Serializer<
  DeleteReviewInstructionDataArgs,
  DeleteReviewInstructionData
> {
  return mapSerializer<
    DeleteReviewInstructionDataArgs,
    any,
    DeleteReviewInstructionData
  >(
    struct<DeleteReviewInstructionData>([['discriminator', u8()]], {
      description: 'DeleteReviewInstructionData',
    }),
    (value) => ({ ...value, discriminator: 3 })
  ) as Serializer<DeleteReviewInstructionDataArgs, DeleteReviewInstructionData>;
}

// Extra Args.
export type DeleteReviewInstructionExtraArgs = { domainName: string };

// Args.
export type DeleteReviewInstructionArgs = DeleteReviewInstructionExtraArgs;

// Instruction.
export function deleteReview(
  context: Pick<Context, 'eddsa' | 'payer' | 'programs'>,
  input: DeleteReviewInstructionAccounts & DeleteReviewInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'lorisTrustpilot',
    '6BvjJHhheqrQpNFkYKAMtd1B7FPTBpxp1QncnwsH3mE7'
  );

  // Accounts.
  const resolvedAccounts = {
    payer: { index: 0, isWritable: true, value: input.payer ?? null },
    reviewer: { index: 1, isWritable: false, value: input.reviewer ?? null },
    domain: { index: 2, isWritable: true, value: input.domain ?? null },
    review: { index: 3, isWritable: true, value: input.review ?? null },
    systemProgram: {
      index: 4,
      isWritable: false,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.domain.value) {
    resolvedAccounts.domain.value = findDomainPda(context, {
      domainName: expectSome(input.domainName),
    });
  }
  if (!resolvedAccounts.review.value) {
    resolvedAccounts.review.value = findReviewPda(context, {
      domain: expectPublicKey(resolvedAccounts.domain.value),
      reviewer: expectPublicKey(resolvedAccounts.reviewer.value),
    });
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDeleteReviewInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
